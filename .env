# Server port
PORT=5000

# The URL your React client runs on (used for CORS during local dev)
CLIENT_URL=http://localhost:5173

# --- SMTP settings ---
# For Gmail: enable 2-Step Verification on the account, then create an
# "App Password" at https://myaccount.google.com/apppasswords and use that
# 16-character password below (NOT your normal Gmail password).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=dixitaman.nov.wwe@gmail.com
SMTP_PASS=dhaf vnup baxw dwhk

# Where the "someone contacted you" notification email is sent
OWNER_EMAIL=dixitaman.nov.wwe@gmail.com

# Name shown as the sender on outgoing emails
FROM_NAME=Aman Dixit
